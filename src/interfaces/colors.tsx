export interface Theme {
  dark: boolean
  colors: Colors[]
}

export interface Colors {
  primary: string
  background: string
  card: string
  text: string
  border: string
  notification: string
  lightPrimary: string
  accent: string
  dimmedText: string
  alert: string
  success: string
  surface: string
  shadow: string
  warn: string
  black: string
}
